<!-- app/views/events/edit.html.erb -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Event Details - Qapture</title>
    <link rel="stylesheet" href="/assets/styles.css"> <!-- Assuming you have a separate stylesheet -->
</head>
<body>
    <div class="container">
        <header>
            <h1>Edit Event: <%= @event.name %></h1>
        </header>

        <%= form_for @event do |f| %>
            <div class="form-group">
                <%= f.label :name, "Event Name" %>
                <%= f.text_field :name, class: "form-control" %>
            </div>

            <div class="form-group">
                <%= f.label :date, "Event Date" %>
                <%= f.date_field :date, class: "form-control" %>
            </div>

            <div class="form-group">
                <%= f.label :location, "Event Location" %>
                <%= f.text_field :location, class: "form-control" %>
            </div>

            <!-- Add any other fields as needed for your event model -->

            <div class="form-actions">
                <%= f.submit "Save Changes", class: "btn btn-primary" %>
                <%= link_to 'Cancel', event_path(@event), class: 'btn btn-secondary' %>
            </div>
        <% end %>

        <footer class="main-footer">
            <p>© 2024 Qapture. Capture, share, and relive your moments.</p>
        </footer>
    </div>
</body>
</html>
